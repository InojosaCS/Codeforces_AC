#include <bits/stdc++.h>
 
using namespace std;

void solve(){
	int64_t a,b,c;
	cin >> a >> b >> c;
	cout << a+b+c-1 << "\n";
}

int32_t main(){
	ios_base::sync_with_stdio(false);cin.tie(0);
	cout.precision(20);
		
	long double tt = 1;
	cin >> tt;

	while(tt-->0) solve();
	
	return 0;
}
